baseurl = "https://masenohub.github.io/blog"
title = "console.log()"
theme = "hugo-nederburg-theme"
languageCode = "en-us"
# Enable comments by entering your Disqus shortname
disqusShortname = "masenohub-blog"
# Enable Google Analytics by entering your tracking code
googleAnalytics = ""
preserveTaxonomyNames = true
paginate = 5 #frontpage pagination
publishDir = "docs"

[privacy]
  # Google Analytics privacy settings - https://gohugo.io/about/hugo-and-gdpr/index.html#googleanalytics
  [privacy.googleAnalytics]
    # set to true to disable service 
    disable = false
    # set to true to meet General Data Protection Regulation (GDPR)
    anonymizeIP = false
    respectDoNotTrack = false
    useSessionStorage = false

[params]
  # Unmark to use post folder for images. Default is static folder.
  #usepostimgfolder = true
  slogan = "The Maseno Hub Blog"
  description = "The official blog of the Maseno Hub community." # meta description
  # You need to set author to have an author bio. If you have a writer in the markdown file it will override this.
  author = "Maseno Hub"
  authorLink = "https://blog.masenohub.io/"
  bio = [
    "Maseno Hub is a community aimed at any individual with an interest in the general subject of technology within the greater Maseno area."
  ]
  copyright = [
    "&copy; 2019 Maseno Hub",
    "Nederburg Hugo Theme by [Appernetic](https://appernetic.io).",
    "A port of Tracks by Compete Themes."
  ]


  # Contact page
     # Since this template is static, the contact form uses www.formspree.io as a
     # proxy. The form makes a POST request to their servers to send the actual
     # email. Visitors can send up to a 1000 emails each month for free.
     #
     # What you need to do for the setup?
     #
     # - set your email address under 'email' below (it is also used in Gravatar for the bio).
     # - upload the generated site to your server
     # - send a dummy email yourself to confirm your account
     # - click the confirm link in the email from www.formspree.io
     # - you're done. Happy mailing!
  email = "maseno.hub@gmail.com"

  # Nav links in the side bar
  [[menu.main]]
  name = "Home"
  url = "/"
  weight = 1

  [[menu.main]]
  name = "About"
  url = "about/"
  weight = 2

  [[menu.main]]
  name = "Get in touch"
  url = "contact/"
  weight = 3

# this will also be in author bio if there is no writer.
  [params.social]
  # facebook      = "full Social profile url in facebook"
  # googleplus    = "full profile url in googleplus"
  email         = "maseno.hub@gmail.com"
  # twitter       = "full profile url in twitter"
  # linkedin      = "full profile url in linkedin"
  # stackoverflow = "full profile url in stackoverflow"
  # instagram     = "full profile url in instagram"
  github        = "https://github.com/masenohub"
  # pinterest     = "full profile url in pinterest"
  rss           = true

# Add a writer, then use it in the markdown front matter like this: writer = "Lukas Herman"
  [params.writers]
    [params.writers."Pete"]
    link = "https://l3njo.github.io"
    # email = "lherman.cs@gmail.com"
    bio = [
      "Pete is a dedicated backend developer, with interest in The Go Programming langage and the .NET platform."
    ]
    # facebook      = "full lukas profile url in facebook"
    # googleplus    = "full profile url in googleplus"
    # twitter       = "full profile url in twitter"
    linkedin      = "https://www.linkedin.com/in/peter-lenjo/"
    stackoverflow = "https://stackoverflow.com/users/9787021/peter-lenjo"
    # instagram     = "full profile url in instagram"
    github        = "https://github.com/l3njo"
    # pinterest     = "full profile url in pinterest"

    [params.writers."Mike"]
    link = "https://kmike-wachira.github.io/kmike-wachira/"
    # email = "goran@molnsys.com"
    bio = [
      "Mike is an excellent Android developer, with extraordinary skill in Java, and a formidable PHP web developer."
    ]
    # facebook      = "full Göran profile url in facebook"
    # googleplus    = "full profile url in googleplus"
    # twitter       = "full profile url in twitter"
    # linkedin      = "full profile url in linkedin"
    # stackoverflow = "full profile url in stackoverflow"
    # instagram     = "full profile url in instagram"
    # github        = "full profile url in github"
    # pinterest     = "full profile url in pinterest"
